export const CSS = `
/* Time Grouping - group stories by age */

/* Group header */
.hwt-time-group > td {
  background: #f6f6ef;
  padding: 8px 12px;
  font-size: 12px;
  font-weight: bold;
  color: #828282;
  border-left: 3px solid #ff6600;
}

/* Make group headers sticky */
.hwt-time-group > td {
  position: sticky;
  top: 0;
  z-index: 10;
}
`;
